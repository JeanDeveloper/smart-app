class DocumentType {
  final int value;
  final String type;
  DocumentType(this.value, this.type);
}

final documentTypes = [
  DocumentType(1, "DNI"),
  DocumentType(2, "Carnet de Extranjeria"),
  DocumentType(3, "Pasaporte"),
];